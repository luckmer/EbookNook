import { Typography } from "@components/Typography";

const Home = () => {
  return (
    <main className="w-full h-full">
      <Typography>Home</Typography>
    </main>
  );
};

export default Home;
