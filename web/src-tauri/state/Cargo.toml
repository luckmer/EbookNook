[package]
name = "state"
version = "0.1.0"
edition = "2024"

[dependencies]
database = { path = "../database" }
notes = { path = "../notes" }
bookmarks = { path = "../bookmarks" }
library = {path ="../library"}
